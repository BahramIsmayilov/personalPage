import React from "react";

const Project = ({ id, image, urlGithub, urlWebSite }) => {
  console.log(id, image, urlGithub, urlWebSite);

  return <div>hell from project</div>;
};

export default Project;
